import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deckbuilder.app',
  appName: 'DeckBuilder',
  webDir: 'dist',
  android: {
    backgroundColor: '#14130f',
  },
};

export default config;
