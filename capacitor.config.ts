import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.buildmate.app',
  appName: 'BuildMate',
  webDir: 'dist',
  android: {
    backgroundColor: '#14130f',
  },
};

export default config;
